diff --git a/data/index.htm b/data/index.htm
index 71899d6..5a59822 100644
--- a/data/index.htm
+++ b/data/index.htm
@@ -1,143 +1,111 @@
<!DOCTYPE html>
<html lang="de">
<head>
  <meta [-charset="UTF-8">-]{+charset="UTF-8" />+}
  <title>Eisenbahn – Safety & Status</title>
  <meta name="viewport" content="width=device-width, [-initial-scale=1.0">-]{+initial-scale=1.0" />+}
  <link rel="stylesheet" [-href="style.css?v=20260106p17">-]{+href="style.css?v=20260107p5">+}
</head>

<body>
<div class="container">

  <!-- [-STATUS BADGES (oben rechts)-]{+LEFT+} -->
  [-<div id="status-badges" class="status-badges">-]
[-        <div class="badge-row">-]
[-            <span id="badge-ws" class="badge badge-warn">WS: ?</span>-]
[-            <span id="badge-mega2" class="badge badge-warn">Mega2: ?</span>-]
[-            <span id="badge-mega1" class="badge badge-warn">Mega1: ?</span>-]
[-        </div>-]
[-        <div class="badge-row">-]
[-            <span id="badge-power" class="badge badge-warn">Power: ?</span>-]
[-            <span id="badge-notaus" class="badge badge-ok">HW-NOT AUS: nein</span>-]
[-        </div>-]
[-    </div>-]{+<main class="main-left">+}
{+    <h1>Betriebsübersicht</h1>+}

    {+<div class="overview-grid">+}

[-<!-- ===================== LEFT ===================== -->-]
[-    <main class="panel">-]
[-        <h1>Betriebsübersicht</h1>-]

[-        <div class="overview-grid">-]      <section class="overview-card">
        <h2>SBHF (Mega2)</h2>
        <div [-id="ov-sbhf">-]
[-                    <em>keine Daten</em>-]
[-                </div>-]
[-            </section>-]

[-            <section class="overview-card">-]
[-                <h2>Blöcke (Mega2)</h2>-]
[-                <div id="ov-blocks">-]
[-                    <em>keine Daten</em>-]
[-                </div>-]
[-            </section>-]

[-            <section class="overview-card">-]
[-                <h2>Weichen (Mega2)</h2>-]
[-                <div id="ov-turnouts">-]
[-                    <em>noch nicht angebunden</em>-]
[-                </div>-]
[-            </section>-]

[-            <section class="overview-card">-]
[-                <h2>Bahnhöfe / Signale (Mega1)</h2>-]
[-                <div id="ov-stations">-]
[-                    <em>noch nicht definiert</em>-]
[-                </div>-]
[-            </section>-]
[-        </div>-]

[-        <div class="hint" style="margin-top: 1rem;">-]
[-            Diagnose/Kalibrier-Seite ist geplant (folgt).-]
[-        </div>-]

[-        <div class="panel" style="margin-top: 1rem;">-]
[-            <h1>Log</h1>-]
[-            <div id="log-window"></div>-]
[-            <div class="hint" style="margin-top:.75rem;">-]
[-                UI-Version: <span id="ui-version">?</span>-]
[-            </div>-]
[-        </div>-]
[-    </main>-]

[-    <!-- ===================== RIGHT / SAFETY ===================== -->-]
[-    <aside class="panel panel-right safety-panel ok" id="safety-panel">-]
[-<!-- POWER (Toggle) -->-]
[-<section class="power-toggle">-]
[-    <button id="btn-power" class="btn-power" onclick="sendPowerOn()">-]
[-        ⚡ POWER ON-]
[-    </button>-]
[-    <div class="hint" style="margin-top:.5rem;">-]
[-        HW-NOT-AUS ist separat verdrahtet (fail-safe).-]
[-    </div>-]
[-</section>-]{+id="ov-sbhf"><em>keine Daten</em></div>+}

        <div [-class="panel-separator"></div>-]{+class="ov-subtitle">Weichen (Mega2)</div>+}
{+        <div id="ov-turnouts"><em>keine Daten</em></div>+}
{+      </section>+}

      [-<!-- STATUS -->-]
[-        <section>-]
[-            <h2>Systemstatus</h2>-]{+<section class="overview-card">+}
{+        <h2>Blöcke (Mega2)</h2>+}
{+        <div id="ov-blocks"><em>keine Daten</em></div>+}
{+      </section>+}

      {+<section class="overview-card">+}
{+        <h2>Bahnhöfe / Signale (Mega1)</h2>+}
        <div [-id="safety-status" class="status-box status-ok">-]
[-                🟢 System OK-]
[-            </div>-]{+id="ov-stations"><em>keine Daten</em></div>+}
      </section>

      {+<section class="overview-card">+}
{+        <h2>Weichen (Mega1)</h2>+}
        <div [-class="panel-separator"></div>-]{+id="ov-m1weichen"><em>keine Daten</em></div>+}
{+      </section>+}

[-<!-- MELDUNGEN -->-]
[-        <section>-]
[-            <h2>Meldungen</h2>-]
[-            <div id="safety-messages-list">-]
[-                <em>Keine Meldungen</em>-]    </div>[-</section>-]

    <div [-class="panel-separator"></div>-]{+class="hint" style="margin-top: 1rem;">+}
{+      Diagnose/Kalibrier-Seite ist geplant (folgt).+}
{+    </div>+}

    [-<!-- TRAFO -->-]
[-        <section>-]
[-            <h2>Trafo</h2>-]
[-            <div>Trafo A: – V</div>-]
[-            <div>Trafo B: – V</div>-]
[-        </section>-]{+<div class="panel" style="margin-top: 1rem;">+}
{+      <h1>Log</h1>+}
{+      <div id="log-window" class="log-window"></div>+}
{+      <div class="hint" style="margin-top:.75rem;">+}
{+        UI-Version: <span id="ui-version">?</span>+}
{+      </div>+}
{+    </div>+}
{+  </main>+}

  [-</aside>-]{+<!-- RIGHT -->+}
{+  <aside class="main-right">+}
{+    <button id="btn-power" class="power-btn power-off" type="button">+}
{+      ⏻ POWER ?+}
{+    </button>+}

    [-</div>-]{+<button id="btn-m1-mode" class="btn btn-mode" type="button">+}
{+      AUTO / MANUELL+}
{+    </button>+}

[-<!-- ===================== ACK OVERLAY ===================== -->-]    <div [-id="ack-overlay" class="overlay hidden">-]{+class="hint" style="margin-top: .75rem;">+}
{+      HW-NOT-AUS ist separat verdrahtet (fail-safe).+}
{+    </div>+}

{+    <hr>+}

{+    <h2>Systemstatus</h2>+}
    <div [-class="overlay-box">-]
[-        <h2 id="ack-title">⚠ Sicherheitsquittierung</h2>-]{+id="system-status" class="system-status ok">+}
{+      <span class="dot"></span>+}
{+      <span id="system-status-text">System OK</span>+}
{+    </div>+}

    [-<div id="ack-text">Sicherheitsfehler</div>-]{+<hr>+}

    [-<label class="ack-checkbox">-]
[-            <input type="checkbox">-]
[-            <span>Fehler vor Ort geprüft und behoben</span>-]
[-        </label>-]{+<h2>Meldungen</h2>+}
{+    <div id="alerts" class="alerts"></div>+}

    {+<hr>+}

{+    <h2>Trafo</h2>+}
    <div [-class="overlay-actions">-]
[-            <button class="btn" onclick="closeOverlay()">Abbrechen</button>-]
[-            <button class="btn btn-ack" onclick="confirmAck()">ACK senden</button>-]{+class="trafo-box">+}
{+      <div>Trafo A: <span id="trafo-a">– V</span></div>+}
{+      <div>Trafo B: <span id="trafo-b">– V</span></div>+}
    </div>
  {+</aside>+}

{+  <!-- OVERLAY unten rechts -->+}
{+  <div id="status-badges" class="status-badges">+}
{+    <div class="badge-row">+}
{+      <span id="badge-ws" class="badge badge-warn">WS: ?</span>+}
{+      <span id="badge-m2" class="badge badge-warn">Mega2: ?</span>+}
{+      <span id="badge-m1" class="badge badge-warn">Mega1: ?</span>+}
{+      <span id="badge-m1mode" class="badge badge-warn">Mega1 Modus: ?</span>+}
    </div>
    {+<div class="badge-row" style="margin-top:6px;">+}
{+      <span id="badge-power" class="badge badge-warn">Power: ?</span>+}
{+      <span id="badge-notaus" class="badge badge-ok">HW-NOT AUS: nein</span>+}
{+    </div>+}
{+  </div>+}

</div>

<script [-src="safety_ui_texts.js?v=20260106p17"></script>-]
[-<script src="script.js?v=20260106p17"></script>-]{+src="script.js?v=20260107p5"></script>+}
</body>
</html>
diff --git a/data/script.js b/data/script.js
index 0397dfc..c92b1a2 100644
--- a/data/script.js
+++ b/data/script.js
@@ -1,665 +1,331 @@
/* =========================================================
 *  Eisenbahn WebUI – [-Safety & Status (WebSocket-only)-]{+WS-only+}
{+ *  Version: 2026-01-07-p5+}
 * ========================================================= */

const [-DEBUG_WS-]{+UI_VERSION+} = [-true;-]{+"2026-01-07-p5";+}
const [-DEBUG_UI-]{+DEBUG_WS+} = false;

[-// Wenn du nach einem Firmware-Flash "alte" Buttons siehst:-]
[-// -> unbedingt auch "Upload File System Image" (UploadFS) ausführen.-]
[-// Diese Version hilft beim Verifizieren, dass Browser + LittleFS wirklich neu sind.-]
[-const UI_VERSION = "2026-01-06-p20-ackpending";-]let [-socket-]{+ws+} = null;
let wsConnected = false;
{+window.lastStateMsg = null;+}

[-// letzter empfangener safety-state vom WS-]
[-let lastSafetyState = null;-]
[-// letzter empfangener mega2-state (online/flags)-]
[-let lastMega2Online = false;-]

[-// letzter kompletter WS-State (für Button/Disable-Regeln)-]
[-let lastStateMsg = null;-]

[-// ACK wurde gesendet, aber Safety-Lock ist (noch) aktiv.-]
[-// Wird zurückgesetzt, sobald safety.lock wieder false ist.-]
[-let ackPending = false;-]

[-/* =========================================================-]
[- *  INIT-]
[- * ========================================================= */-]

[-window.addEventListener("load", () => {-]
[-  const v = document.getElementById("ui-version");-]
[-  if (v) v.textContent = UI_VERSION;-]
[-  console.log("[UI] version", UI_VERSION);-]
[-  connectWebSocket();-]
[-});-]

[-/* =========================================================-]
[- *  WS CONNECT-]
[- * ========================================================= */-]function [-connectWebSocket() {-]
[-  socket = new WebSocket(`ws://${location.host}/ws`);-]

[-  socket.onopen = () =>-]{+$(id)+} { [-wsConnected = true;-]
[-    logLine("WS connected");-]
[-    if (lastStateMsg) {-]
[-      const uiState = getUiStateFromWs(lastStateMsg, lastSafetyState, lastMega2Online);-]
[-      applyUiState(uiState, lastStateMsg);-]{+return document.getElementById(id);+} }[-};-]

[-  socket.onclose = () => {-]
[-    wsConnected = false;-]
[-    logLine("WS closed - retry...");-]
[-    if (lastStateMsg) {-]
[-      const uiState = getUiStateFromWs(lastStateMsg, lastSafetyState, lastMega2Online);-]
[-      applyUiState(uiState, lastStateMsg);-]
[-    }-]
[-    setTimeout(connectWebSocket, 1000);-]
[-  };-]

[-  socket.onerror = () => {-]
[-    // onclose kommt meist danach sowieso, aber fürs Log ok-]
[-    wsConnected = false;-]
[-    logLine("WS error");-]
[-  };-]

[-socket.onmessage = (ev) => {-]
[-    try-]{+function logLine(s)+} {
  const [-msg-]{+w+} = [-JSON.parse(ev.data);-]
[-      handleWsMessage(msg);-]
[-    } catch (e) {-]
[-      console.warn("WS parse error", e);-]
[-    }-]
[-  };-]{+$("log-window");+}
{+  if (!w) return;+}
{+  const line = document.createElement("div");+}
{+  line.textContent = s;+}
{+  w.prepend(line);+}
}

[-/* =========================================================-]
[- *  WS SEND (Actions)-]
[- * ========================================================= */-]{+function bit(mask, i) {+}
{+  return (((mask ?? 0) >> i) & 1) === 1;+}
{+}+}

function wsSend(obj) {
  if [-(!socket-]{+(!wsConnected+} || [-wsConnected !== true) {-]
[-    logLine("WS nicht verbunden – Aktion nicht möglich");-]{+!ws)+} return false;[-}-]
  try { [-socket.send(JSON.stringify(obj));-]{+ws.send(JSON.stringify(obj));+} return true; }
  catch (e) {[-console.warn("WS send error", e);-]
[-    logLine("WS send error");-] return false; }
}

[-function sendPollNow() {-]
[-  wsSend({ action: "pollNow" });-]
[-  logLine("↻ Prüfen gesendet");-]
[-}-]{+/* ===================== ACTIONS ===================== */+}

{+function sendPowerToggle() {+}
{+  // Backend: queued actions -> I2C+}
{+  const ok = wsSend({ action: "powerToggle" });+}
{+  if (ok) logLine("POWER Toggle gesendet");+}
{+}+}

function [-wsSendAction(action, okMsg)-]{+sendMega1ModeToggle()+} {
  const {+d = window.lastStateMsg?.mega1?.diag;+}
{+  if (!d) { logLine("Mega1 Mode: keine Daten"); return; }+}
{+  const mode = Number(d.mode ?? 0);     // 0=MANUELL, 1=AUTO+}
{+  const target = (mode === 1) ? 0 : 1;  // toggle+}
{+  const+} ok = wsSend({ action: [-action-]{+"m1SetMode", mode: target+} });
  if [-(ok && okMsg) logLine(okMsg);-]{+(ok) logLine(`Mega1 Modus -> ${target === 1 ? "AUTO" : "MANUELL"} gesendet`);+}
}

[-/* =========================================================-]
[- *  WS MESSAGE HANDLER-]
[- * ========================================================= */-]{+function sendMega1WeicheToggle(idx) {+}
{+  const d = window.lastStateMsg?.mega1?.diag;+}
{+  if (!d) { logLine("Mega1 Weiche: keine Daten"); return; }+}

  [-function handleWsMessage(msg) {-]
[-  if (DEBUG_WS) console.log("[WS MSG]", msg);-]
[-  if (!msg || msg.type !== "state") return;-]{+const istBits = d.weicheIstBits ?? d.weicheIstGeradeBits ?? 0;+}
{+  const istGerade = bit(istBits, idx);+}
{+  const targetGerade = !istGerade;+}

  [-lastSafetyState-]{+const ok+} = [-msg.safety || null;-]
[-	// Sobald Safety-Lock wieder weg ist, ist ein evtl. laufender Quittierungs-/Test-Flow beendet.-]{+wsSend({ action: "m1TurnoutSet", idx, gerade: targetGerade });+}
  if [-(lastSafetyState && lastSafetyState.lock === false) {-]
[-	  ackPending = false;-]{+(ok) logLine(`Mega1 Weiche W${idx} -> ${targetGerade ? "GERADE" : "ABB"} gesendet`);+}
}[-lastMega2Online = !!(msg.mega2 && msg.mega2.online);-]
[-  lastStateMsg = msg;-]

{+function sendMega1BhfToggle(bhfIdx) {+}
  const [-uiState-]{+d+} = [-getUiStateFromWs(msg, lastSafetyState, lastMega2Online);-]
[-  applyUiState(uiState, msg);-]{+window.lastStateMsg?.mega1?.diag;+}
{+  if (!d) { logLine("Mega1 Bahnhof: keine Daten"); return; }+}

  [-// Schritt 2: rechts "Meldungen" befüllen (Safety + SBHF Masken)-]
[-  renderPowerWarningsEmergencies(msg);-]{+const pm = d.powerMask ?? 0;+}
{+  const on = bit(pm, bhfIdx);+}
{+  const targetOn = !on;+}

  [-// Schritt 3.5: links Betriebsübersicht (SBHF/Blöcke/Weichen)-]{+const ok = wsSend({ action: "m1PowerSet", bhf: bhfIdx, on: targetOn });+}
{+  if (ok) logLine(`Mega1 Bahnhof B${bhfIdx+} + [-FROM→TO Signale-]
[-  renderOverviewLeft(msg);-]{+1} -> ${targetOn ? "GRÜN" : "ROT"} gesendet`);+}
}

/* [-=========================================================-]
[- *  UI STATE FROM WS-]
[- * =========================================================-]{+===================== RENDER =====================+} */[-function getSafetyOverlayTexts(safety) {-]
[-  try {-]
[-    // Prefer numeric codes (errType/errIndex) -> text mapping from safety_ui_texts.js-]
[-    if (window.SAFETY_UI_TEXTS && typeof window.SAFETY_UI_TEXTS.fromCodes === 'function') {-]
[-      const t = window.SAFETY_UI_TEXTS.fromCodes(safety?.errType, safety?.errIndex);-]
[-      if (t && (t.title || (t.lines && t.lines.length))) {-]
[-        return { title: t.title || '⚠ Sicherheitsquittierung', lines: t.lines || [] };-]
[-      }-]
[-    }-]
[-  } catch (e) {-]
[-    console.warn('SAFETY_UI_TEXTS error', e);-]
[-  }-]
[-  // Fallback: backend-provided text-]
[-  if (safety && safety.text) {-]
[-    return { title: '⚠ Sicherheitsquittierung', lines: [String(safety.text)] };-]
[-  }-]
[-  return { title: '⚠ Sicherheitsquittierung', lines: ['🔴 Safety aktiv – Bedienung gesperrt'] };-]
[-}-]

function [-getUiStateFromWs(msg, safety, mega2online)-]{+renderBadges(msg)+} {
  [-// Default OK-]
[-  let level-]{+const m2+} = [-"OK";-]
[-  let text-]{+!!msg?.mega2?.online;+}
{+  const m1+} = [-["🟢 System OK"];-]
[-  let title-]{+!!msg?.mega1?.online;+}
{+  const pwr+} = [-"";-]
[-  let overlay = false;-]
[-  let ackRequired = false;-]
[-  let hasWarn = false;-]

[-  if (!mega2online) {-]
[-    level = "WARN";-]
[-    text = ["🟡 Mega2 offline"];-]
[-    return { level, text, title, overlay, ackRequired, hasWarn };-]
[-  }-]
[-  // Safety lock dominates everything-]
[-  if (safety && safety.lock === true) {-]
[-    level = 'ERR';-]
[-    overlay = true;-]
[-    ackRequired = true;-]{+!!msg?.safety?.powerOn;+}

  const [-t-]{+bWs+} = [-getSafetyOverlayTexts(safety);-]
[-    title-]{+$("badge-ws");+}
{+  const bM2 = $("badge-m2");+}
{+  const bM1+} = [-t.title || '⚠ Sicherheitsquittierung';-]
[-    text-]{+$("badge-m1");+}
{+  const bPw = $("badge-power");+}
{+  const bMode+} = [-(t.lines && t.lines.length) ? t.lines : ['🔴 Safety aktiv – Bedienung gesperrt'];-]{+$("badge-m1mode");+}

  [-return-]{+if (bWs)+} {
    [-level, text, title, overlay, ackRequired, hasWarn };-]{+bWs.className = "badge " + (wsConnected ? "badge-ok" : "badge-err");+}
{+    bWs.textContent = "WS: " + (wsConnected ? "verbunden" : "getrennt");+}
  }
[-// Warnings (z.B. Weichenfehler / Restricted Mode) -> Systemstatus = WARNING-]
[-  const sb = msg && msg.mega2 && msg.mega2.sbhf;-]
[-  if (sb) {-]
[-    const allowed = (sb.allowedMask ?? 0) & 0xff;-]
[-    const warn    = (sb.warningMask ?? 0) & 0xff;-]

[-    const restricted = (allowed !== 0x07 && allowed !== 0x00);-]
[-    hasWarn = (warn !== 0) || restricted;-]  if [-(hasWarn)-]{+(bM2)+} {
    [-level = "WARN";-]
[-      text = ["🟡 Warning aktiv"];-]
[-    }-]{+bM2.className = "badge " + (m2 ? "badge-ok" : "badge-err");+}
{+    bM2.textContent = "Mega2: " + (m2 ? "online" : "offline");+}
  }
  [-return { level, text, title, overlay, ackRequired, hasWarn };-]
[-}-]

[-/* =========================================================-]
[- *  UI APPLY-]
[- * ========================================================= */-]

[-function applyUiState(ui, msg)-]{+if (bM1)+} {
    [-const panel-]{+bM1.className+} = [-document.getElementById("safety-panel");-]
[-  const status-]{+"badge " + (m1 ? "badge-ok" : "badge-err");+}
{+    bM1.textContent+} = [-document.getElementById("safety-status");-]{+"Mega1: " + (m1 ? "online" : "offline");+}
{+  }+}
  if [-(!panel || !status) return;-]

[-  panel.className-]{+(bPw) {+}
{+    bPw.className+} = [-"safety-panel-]{+"badge+} " + [-ui.level.toLowerCase();-]
[-  status.textContent-]{+(pwr ? "badge-ok" : "badge-warn");+}
{+    bPw.textContent+} = [-ui.text[0] || "";-]

[-  if (ui.overlay) {-]
[-    showOverlay(ui.title, ui.text, ui.ackRequired);-]
[-  } else {-]
[-    hideOverlay();-]{+"Power: " + (pwr ? "AN" : "aus");+}
  }

  // [----------------------------------------------------]
[-  // Disable rules + states-]
[-  // ---------------------------------------------------]

[-  const mega2online = !!(msg && msg.mega2 && msg.mega2.online);-]
[-const mega1online = !!(msg.mega1 && msg.mega1.online);-]
[-  const wsOk = (wsConnected === true);-]
[-  const lock = !!(lastSafetyState && lastSafetyState.lock === true);-]
[-  const notausActive = !!(lastSafetyState && lastSafetyState.notausActive === true);-]
[-  const powerOn = !!(lastSafetyState && lastSafetyState.powerOn === true);-]

[-// ---------------------------------------------------]
[-// Status-Badges (oben rechts)-]
[-// ---------------------------------------------------]
[-const bWs = document.getElementById("badge-ws");-]
[-const bM2 = document.getElementById("badge-mega2");-]
[-const bM1 = document.getElementById("badge-mega1");-]
[-const bPw = document.getElementById("badge-power");-]
[-const bNo = document.getElementById("badge-notaus");-]

[-if (bWs) {-]
[-  bWs.className = "badge " + (wsOk ? "badge-ok" : "badge-err");-]
[-  bWs.textContent = "WS: " + (wsOk ? "verbunden" : "getrennt");-]
[-}-]{+Mode badge color: green=AUTO, blue=MANUELL+}
  if [-(bM2)-]{+(bMode)+} {[-bM2.className = "badge " + (mega2online ? "badge-ok" : "badge-err");-]
[-  bM2.textContent = "Mega2: " + (mega2online ? "online" : "offline");-]
[-}-]
[-if (bM1) {-]
[-  bM1.className = "badge " + (mega1online ? "badge-ok" : "badge-err");-]
[-  bM1.textContent = "Mega1: " + (mega1online ? "online" : "offline");-]
[-}-]
[-if (bPw) {-]
[-  bPw.className = "badge " + (powerOn ? "badge-ok" : "badge-warn");-]
[-  bPw.textContent = "Power: " + (powerOn ? "AN" : "aus");-]
[-}-]
[-if (bNo) {-]
[-  bNo.className = "badge " + (notausActive ? "badge-err" : "badge-ok");-]
[-  bNo.textContent = "HW-NOT AUS: " + (notausActive ? "AKTIV" : "nein");-]
[-}-]


[-  // STOP (UI) – immer erlaubt (wenn WS verbunden), auch bei Safety lock.-]
[-// (STOP ist UI-Command "PowerOff". HW-NOT-AUS ist rein Anzeige.)-]

[-// POWER – wenn Mega2 offline, alles außer STOP sperren.-]

[-  // POWER – wenn Mega2 offline, alles außer STOP sperren.-]
    const [-btnPower = document.getElementById("btn-power");-]
[-  if (btnPower) {-]
[-    btnPower.textContent-]{+d+} = [-powerOn ? "⏻ STOP / POWER OFF" : "⚡ POWER ON";-]
[-    btnPower.classList.toggle("is-on", powerOn);-]
[-    btnPower.classList.toggle("is-off", !powerOn);-]
[-    btnPower.classList.toggle("is-offline", !mega2online);-]

[-    // Regeln:-]
[-    // - Wenn WS down oder Mega2 offline: disable-]
[-    // - Wenn Power bereits an: POWER OFF erlauben-]
[-    // - Wenn Power aus: POWER ON nur erlauben, wenn nicht gelockt und HW-NOT-AUS nicht aktiv-]{+msg?.mega1?.diag;+}
    if [-(!wsOk-]{+(!m1+} || [-!mega2online)-]{+!d || typeof d.mode !== "number")+} {
      [-btnPower.disabled-]{+bMode.className+} = [-true;-]
[-    } else if (powerOn) {-]
[-      btnPower.disabled-]{+"badge badge-warn";+}
{+      bMode.textContent+} = [-false;-]{+"Mega1 Modus: ?";+}
    } else {
      [-btnPower.disabled-]{+const isAuto+} = [-(lock || notausActive);-]{+(d.mode === 1);+}
{+      bMode.className = "badge " + (isAuto ? "badge-green" : "badge-blue");+}
{+      bMode.textContent = "Mega1 Modus: " + (isAuto ? "AUTO" : "MANUELL");+}
    }
  }
[-}-]

  [-/* =========================================================-]
[- *  ACTIONS (WebSocket-only)-]
[- * ========================================================= */-]

[-function sendNothalt()-]{+// Top-right buttons+}
{+  const btnPower = $("btn-power");+}
{+  if (btnPower)+} {
    [-wsSendAction("nothalt", "NOTAUS gesendet");-]{+btnPower.classList.toggle("power-on", !!pwr);+}
{+    btnPower.classList.toggle("power-off", !pwr);+}
{+    btnPower.textContent = pwr ? "⏻ STOP / POWER OFF" : "⚡ POWER ON";+}
{+  }+}
}

{+function renderMega2Sbhf(msg) {+}
{+  const el = $("ov-sbhf");+}
{+  if (!el) return;+}
{+  const sb = msg?.mega2?.sbhf;+}
{+  if (!msg?.mega2?.online || !sb) { el.innerHTML = "<em>keine Daten</em>"; return; }+}

[-function sendPowerOn() {-]  const [-powerOn-]{+allowed+} = [-!!(lastSafetyState && lastSafetyState.powerOn === true);-]

[-  // Verbindung prüfen (damit der Klick nicht "ins Leere" läuft)-]
[-  if (!wsConnected || !socket || socket.readyState !== 1) {-]
[-    logLine("WS nicht verbunden – Aktion nicht gesendet");-]
[-    return;-]
[-  }-]

[-  // Mega2-Online prüfen (optional: STOP bleibt trotzdem erlaubt, aber PowerToggle nicht)-]
[-  if (!lastMega2Online) {-]
[-    logLine("Mega2 offline – Aktion nicht gesendet");-]
[-    return;-]
[-  }-]{+msg?.mega2?.allowedMask ?? sb.allowedMask;+}
{+  const restricted = !!(sb.restricted);+}

  [-const notausActive-]{+// minimal – du kannst hier später mehr reinrendern+}
{+  el.innerHTML+} = [-!!(lastSafetyState && lastSafetyState.notausActive === true);-]
[-  const safetyLock   = !!(lastSafetyState && lastSafetyState.lock === true);-]{+`+}
{+    <div class="kv">State: <b>${sb.state ?? "?"}</b></div>+}
{+    <div class="kv">Ausfahr-Gleis: <b>${sb.currentTrack ?? sb.gleis ?? "?"}</b></div>+}
{+    <div class="kv">Belegt: <b>${sb.occupiedTrack ?? "—"}</b></div>+}
{+    <div class="kv">Erlaubt: <b>${sb.allowedText ?? "G1, G2, G3"}</b></div>+}
{+    <div class="kv">Restricted: <b>${restricted ? "ja" : "nein"}</b></div>+}
{+  `;+}
{+}+}

[-// Toggle-Logik:-]
[-  // - wenn Power bereits AN -> UI-STOP-]{+function renderMega2Turnouts(msg) {+}
{+  const el+} = [-PowerOff-]
[-  // - wenn Power AUS -> nur PowerOn, wenn kein Safety-Lock und kein HW-NOT-AUS-]{+$("ov-turnouts");+}
  if [-(powerOn) {-]
[-    wsSendAction("powerOff", "STOP / POWER OFF gesendet");-]{+(!el)+} return;[-}-]

  {+const t = msg?.mega2?.turnouts;+}
  if [-(safetyLock-]{+(!msg?.mega2?.online+} || [-notausActive)-]{+!t)+} { [-showAckOverlay(-]
[-      (lastSafetyState && lastSafetyState.text) ||-]
[-        "Power On nicht möglich – Safety aktiv-]{+el.innerHTML = "<em>keine Daten</em>"; return; }+}

{+  // erwartet: t.items+} oder [-HW-NOT-AUS"-]
[-    );-]{+feste W12..W15+}
{+  const items = t.items ?? t.list ?? [];+}
{+  if (items.length === 0 && typeof t === "object") {+}
{+    // Fallback: W12..W15+}
{+    const fallback = ["W12","W13","W14","W15"].map((name, i) => ({+}
{+      name,+}
{+      soll: t[`w${12+i}Soll`] ?? t[`W${12+i}Soll`] ?? "G",+}
{+      ist:  t[`w${12+i}Ist`]  ?? t[`W${12+i}Ist`]  ?? "G",+}
{+      ok:   true+}
{+    }));+}
{+    el.innerHTML = `<div class="pill-row">` + fallback.map(x =>+}
{+      `<span class="pill pill-ro ${x.ok ? "ok" : "bad"}">${x.name} Soll:${x.soll} Ist:${x.ist}</span>`+}
{+    ).join("") + `</div>`;+}
    return;
  }

  [-wsSendAction("powerOn", "POWER ON gesendet");-]{+el.innerHTML =+}
{+    `<div class="pill-row">` ++}
{+    items.map(x => {+}
{+      const ok = (x.ok ?? (x.soll === x.ist));+}
{+      return `<span class="pill pill-ro ${ok ? "ok" : "bad"}">${x.name} Soll:${x.soll} Ist:${x.ist}</span>`;+}
{+    }).join("") ++}
{+    `</div>`;+}
}

{+function renderMega2Blocks(msg) {+}
{+  const el = $("ov-blocks");+}
{+  if (!el) return;+}

[-/* =========================================================-]
[- *  ACK OVERLAY-]
[- * ========================================================= */-]

[-function showOverlay(title, lines, requireChecked) {-]
[-  const overlay = document.getElementById("ack-overlay");-]
[-  const titleEl = document.getElementById("ack-title");-]  const [-textEl-]{+blocks+} = [-document.getElementById("ack-text");-]
[-  const checkbox = overlay?.querySelector("input[type=checkbox]");-]
[-  const ackBtn = overlay?.querySelector(".btn-ack");-]{+msg?.mega2?.blocks;+}
  if [-(!overlay || !titleEl-]{+(!msg?.mega2?.online+} || [-!textEl) return;-]

[-  const wasHidden = overlay.classList.contains("hidden");-]

[-  overlay.classList.remove("hidden");-]

[-  titleEl.textContent = title || "⚠ Sicherheitsquittierung";-]

[-  const safeLines = (Array.isArray(lines) ? lines : [lines])-]
[-    .filter(Boolean)-]
[-    .map((l) => escapeHtml(String(l)));-]

[-  textEl.innerHTML-]{+!blocks) { el.innerHTML+} = [-safeLines.join("<br>");-]{+"<em>keine Daten</em>"; return; }+}

  // [-Wenn bereits ACK gesendet wurde, aber der Safety-Lock noch aktiv ist,-]
[-  // dann läuft (z.B. im SBHF) typischerweise ein automatischer Selbsttest.-]
[-  // In dieser Phase darf die Checkbox/ACK nicht weiter bedient werden.-]
[-  if (ackPending) {-]
[-    titleEl.textContent = "🔄 SBHF Weichentest läuft";-]
[-    textEl.innerHTML = [-]
[-      "Bitte warten …",-]
[-      "Der Selbsttest läuft im Hintergrund und wird automatisch abgeschlossen.",-]
[-    ].map((l) => escapeHtml(String(l))).join("<br>");-]
[-    if (checkbox) {-]
[-      checkbox.disabled = true;-]
[-    }-]
[-    if (ackBtn) {-]
[-      ackBtn.disabled = true;-]
[-      ackBtn.textContent = "Bitte warten …";-]
[-    }-]
[-    return;-]
[-  }-]{+Minimal: mask free/occupied (wenn vorhanden)+}
{+  const occMask = blocks.occupiedMask ?? msg?.mega2?.blockOccupiedMask ?? 0;+}

  [-// ACK senden ist nur sinnvoll, wenn WS ok und Mega2 online.-]
[-  const canAckSend = (wsConnected === true) && (lastMega2Online === true);-]
[-  if (ackBtn) {-]
[-    ackBtn.disabled-]{+let html+} = [-!canAckSend;-]
[-    ackBtn.textContent-]{+`<div class="ov-subtitle">Belegung:</div><div class="pill-row">`;+}
{+  for (let i=1;i<=9;i++){+}
{+    const occ+} = [-"ACK";-]{+bit(occMask, i-1);+}
{+    html += `<span class="pill pill-ro ${occ ? "bad":"ok"}">B${i} ${occ?"belegt":"frei"}</span>`;+}
  }
  {+html += `</div>`;+}

[-if (checkbox) {-]  // [-Wichtig: Checkbox ist User-Interaktion. Nicht bei jedem WS-State-Update zurücksetzen.-]{+optional: signals array+}
  if [-(wasHidden)-]{+(blocks.signals && Array.isArray(blocks.signals)) {+}
{+    html += `<div class="ov-subtitle" style="margin-top:8px;">Signale (FROM → TO):</div><div class="pill-row">`;+}
{+    html += blocks.signals.map(s =>+} {
      [-checkbox.checked-]{+const ok+} = [-false;-]
[-    }-]
[-    checkbox.disabled-]{+(s.n === "OK" || s.n === 0);+}
{+      const txt+} = [-!requireChecked;-]{+`P: ${s.p}  N: ${s.nTxt ?? s.n ?? "?"}`;+}
{+      return `<span class="pill pill-ro ${ok ? "ok":"bad"}">${txt}</span>`;+}
{+    }).join("");+}
{+    html += `</div>`;+}
  }
[-}-]

[-function hideOverlay() {-]
[-  const overlay = document.getElementById("ack-overlay");-]
[-  if (!overlay) return;-]
[-  overlay.classList.add("hidden");-]
[-}-]

  [-function closeOverlay() {-]
[-  hideOverlay();-]{+el.innerHTML = html;+}
}

function [-confirmAck()-]{+renderMega1Stations(msg)+} {
  const [-overlay-]{+el+} = [-document.getElementById("ack-overlay");-]
[-  const checkbox = overlay?.querySelector("input[type=checkbox]");-]{+$("ov-stations");+}
  if [-(checkbox && checkbox.disabled === false && checkbox.checked === false) {-]
[-    logLine("Bitte vor Ort prüfen und Checkbox bestätigen.");-]{+(!el)+} return;[-}-]

  const [-ok-]{+online+} = [-wsSend({ action: "safetyAck" });-]{+!!msg?.mega1?.online;+}
{+  const d = msg?.mega1?.diag;+}
  if [-(ok)-]{+(!online || !d)+} { [-logLine("ACK gesendet – Selbsttest läuft …");-]
[-    ackPending-]{+el.innerHTML = "<em>keine Daten</em>"; return; }+}

{+  const pm = d.powerMask ?? 0;+}

{+  let html = `<div class="pill-row">`;+}
{+  for (let i=0;i<4;i++){+}
{+    const on+} = [-true;-]
[-    // Overlay absichtlich offen lassen, aber UI sperren + "läuft" anzeigen.-]
[-    showOverlay("🔄 SBHF Weichentest läuft", ["Bitte warten …"], false);-]{+bit(pm, i);+}
{+    html += `+}
{+      <button type="button"+}
{+        class="pill pill-btn ${on ? "ok":"bad"}"+}
{+        onclick="sendMega1BhfToggle(${i})">+}
{+        B${i+1}: ${on ? "GRÜN" : "ROT"}+}
{+      </button>`;+}
  }
  {+html += `</div>`;+}
{+  el.innerHTML = html;+}
}

[-/* =========================================================-]
[- *  LOG-]
[- * ========================================================= */-]function [-logLine(txt)-]{+renderMega1Weichen(msg)+} {
  const [-win-]{+host+} = [-document.getElementById("log-window");-]{+$("ov-m1weichen");+}
  if [-(!win)-]{+(!host)+} return;

  const [-p-]{+online+} = [-document.createElement("div");-]
[-  p.textContent-]{+!!msg?.mega1?.online;+}
{+  const d = msg?.mega1?.diag;+}
{+  if (!online || !d) { host.innerHTML = "<em>keine Daten</em>"; return; }+}

{+  const istBits  = d.weicheIstBits  ?? d.weicheIstGeradeBits  ?? 0;+}
{+  const sollBits = d.weicheSollBits ?? d.weicheSollGeradeBits ?? 0;+}
{+  const slowBits = d.weicheSlowBits ?? d.weicheSlowActiveBits ?? 0;+}

{+  let html = `<div class="pill-grid">`;+}

{+  for (let i=0;i<12;i++){+}
{+    const istG = bit(istBits, i);+}
{+    const solG+} = [-txt;-]
[-  win.appendChild(p);-]
[-  win.scrollTop-]{+bit(sollBits, i);+}
{+    const slow = bit(slowBits, i);+}
{+    const ok = (istG === solG);+}

{+    // Debug: "R" + Rohbit+}
{+    const dbg+} = [-win.scrollHeight;-]{+slow ? `<span class="dbg dbg-red" title="Reduktions-Relais aktiv">R</span><span class="dbg dbg-raw">r=1</span>`+}
{+                     : `<span class="dbg dbg-raw">r=0</span>`;+}

{+    html += `+}
{+      <button type="button"+}
{+        class="pill pill-btn pill-m1 ${ok ? "ok":"bad"} ${slow ? "slow":""}"+}
{+        onclick="sendMega1WeicheToggle(${i})">+}
{+        <span class="t">W${i}</span>+}
{+        <span class="s">Ist:${istG ? "G":"A"}</span>+}
{+        <span class="s">Soll:${solG ? "G":"A"}</span>+}
{+        ${slow ? `<span class="s">SLOW</span>` : ``}+}
{+        ${dbg}+}
{+      </button>`;+}
  }[-/* =========================================================-]
[- *  HELPER-]
[- * ========================================================= */-]

  [-function escapeHtml(str) {-]
[-  return str-]
[-    .replaceAll("&", "&amp;")-]
[-    .replaceAll("<", "&lt;")-]
[-    .replaceAll(">", "&gt;")-]
[-    .replaceAll('"', "&quot;")-]
[-    .replaceAll("'", "&#039;");-]{+html += `</div>`;+}
{+  host.innerHTML = html;+}
}

[-/* =========================================================-]
[- *  Schritt 2: POWER / WARNINGS / EMERGENCIES (rechts)-]
[- * ========================================================= */-]function [-renderPowerWarningsEmergencies(msg)-]{+renderAll(msg)+} {
  [-const el = document.getElementById("safety-messages-list");-]
[-  if (!el) return;-]

[-  const items = [];-]

[-  // 1) Emergencies / Safety-Text-]
[-  const safety = msg && msg.safety;-]
[-  if (safety && safety.text) {-]
[-    items.push(`⛔ ${escapeHtml(String(safety.text))}`);-]
[-  }-]{+renderBadges(msg);+}
{+  renderMega2Sbhf(msg);+}
{+  renderMega2Turnouts(msg);+}
{+  renderMega2Blocks(msg);+}
{+  renderMega1Stations(msg);+}
{+  renderMega1Weichen(msg);+}

  // [-2) Mega2 SBHF Masken-]
[-  const m2 = msg && msg.mega2;-]
[-  const sb = m2 && m2.sbhf;-]{+Alerts minimal (du hast ggf. schon mehr serverseitig)+}
{+  const alerts = $("alerts");+}
  if [-(m2 && m2.online && sb)-]{+(alerts)+} {
    const [-allowed = (sb.allowedMask ?? 0) & 0xff;-]
[-    const warn    = (sb.warningMask ?? 0) & 0xff;-]

[-    // Bits (Contract)-]
[-    const WARN_RESTRICTED_MODE      = 0x01;-]
[-    const WARN_W12_DEFECT           = 0x02;-]
[-    const WARN_W13_DEFECT           = 0x04;-]
[-    const WARN_W14_DEFECT           = 0x08;-]
[-    const WARN_W15_DEFECT           = 0x10;-]
[-    const WARN_SBH_SERVICE_REQUIRED = 0x20;-]

[-    // Allowed tracks-]
[-    const tracks-]{+lines+} = [];
    if [-(allowed & 0x01) tracks.push("G1");-]
[-    if (allowed & 0x02) tracks.push("G2");-]
[-    if (allowed & 0x04) tracks.push("G3");-]

[-    if (allowed === 0x00) {-]
[-      items.push("⛔ SBHF gesperrt (kein sicherer Pfad)");-]
[-    } else-]{+(msg?.mega2?.online && msg?.mega2?.allowedMask != null)+} {
      [-items.push(`🚦 SBHF-]{+lines.push(`SBHF+} erlaubte Gleise: [-${tracks.length ? tracks.join(", ") : "—"}`);-]{+${msg?.mega2?.allowedText ?? "G1, G2, G3"}`);+}
    }
    [-const restricted-]{+alerts.innerHTML+} = [-((warn & WARN_RESTRICTED_MODE) !== 0) ||-]
[-      (allowed !== 0x07 && allowed !== 0x00);-]

[-    if (restricted) items.push("⚠ SBHF: Restricted Mode aktiv");-]
[-    if (warn & WARN_W12_DEFECT) items.push("⚠ W12 defekt");-]
[-    if (warn & WARN_W13_DEFECT) items.push("⚠ W13 defekt");-]
[-    if (warn & WARN_W14_DEFECT) items.push("ℹ W14 Störung");-]
[-    if (warn & WARN_W15_DEFECT) items.push("ℹ W15 Störung");-]
[-    if (warn & WARN_SBH_SERVICE_REQUIRED) items.push("🛠 Service erforderlich");-]{+lines.length ? lines.map(s => `<div class="alert-line">🚦 ${s}</div>`).join("") : "<em>—</em>";+}
  }

[-  -]

[-// 3) "↻ Prüfen" (PollNow), wenn Warnings/Restricted aktiv sind-]
[-let warningActive = false;-]
[-if (m2 && m2.sbhf) {-]
[-  const warn = Number(m2.sbhf.warningMask || 0);-]
[-  const allowed = Number(m2.sbhf.allowedMask || 0);-]
[-	  const restrictedFlag = !!m2.sbhf.restricted;-]
[-	  const restricted =-]
[-	    restrictedFlag ||-]
[-	    (warn !== 0) ||-]
[-	    (allowed !== 0x07 && allowed !== 0x00);-]
[-	  warningActive = restricted;-]
[-}-]

[-const canPollNow = wsConnected && !!(msg && msg.mega2 && msg.mega2.online);-]
[-const pollBtnHtml = warningActive-]
[-  ? `<div class="msg-actions"><button class="btn-mini" ${canPollNow ? "" : "disabled"} onclick="sendPollNow()">↻ Prüfen</button></div>`-]
[-  : "";-]

[-  el.innerHTML = (items.length ? items.map(t => `<div>${t}</div>`).join("") : "<em>Keine Meldungen</em>") + pollBtnHtml;-]
[-}-]

[-/* =========================================================-]
[- *  Schritt 3.5: Links – Betriebsübersicht + Block-Signale-]
[- * ========================================================= */-]

[-function bit(mask, i) {-]
[-  return ((mask >>> i) & 1) !== 0;-]
[-}-]

[-function renderOverviewLeft(msg) {-]
[-  renderSbhfLeft(msg);-]
[-  renderTurnoutsLeft(msg);-]
[-  renderBlocksLeft(msg);-]
[-  // Mega1 Stations folgt später-]
}

[-function renderSbhfLeft(msg) {-]
[-  const el = document.getElementById("ov-sbhf");-]
[-  if (!el) return;-]

[-  const online = !!msg?.mega2?.online;-]
[-  const sb = msg?.mega2?.sbhf;-]

[-  if (!online || !sb) {-]
[-    el.innerHTML = "<em>keine Daten</em>";-]
[-    return;-]
[-  }-]

[-  const state = sb.state ?? 0;-]
[-  const g = sb.currentGleis ?? 0;-]
[-  const occ = sb.occupiedMask ?? 0;-]
[-  const allowed = (sb.allowedMask ?? 0) & 0xff;-]
[-  const restricted = !!sb.restricted;-]

[-  const occList = [];-]
[-  if (occ & 0x01) occList.push("G1");-]
[-  if (occ & 0x02) occList.push("G2");-]
[-  if (occ & 0x04) occList.push("G3");-]{+/* ===================== WS ===================== */+}

{+function wsConnect() {+}
  const [-allowList-]{+proto+} = [-[];-]
[-  if (allowed & 0x01) allowList.push("G1");-]
[-  if (allowed & 0x02) allowList.push("G2");-]
[-  if (allowed & 0x04) allowList.push("G3");-]

[-  el.innerHTML = `-]
[-    <div>State: <b>${state}</b></div>-]
[-    <div>Ausfahr-Gleis: <b>${g}</b></div>-]
[-    <div>Belegt: ${occList.length ? occList.join(", ") : "—"}</div>-]
[-    <div>Erlaubt: ${allowList.length ? allowList.join(", ") : "—"}</div>-]
[-    <div>Restricted: <b>${restricted-]{+(location.protocol === "https:")+} ? [-"ja"-]{+"wss"+} : [-"nein"}</b></div>-]
[-  `;-]
[-}-]{+"ws";+}
{+  const url = `${proto}://${location.host}/ws`;+}
{+  ws = new WebSocket(url);+}

  [-function renderTurnoutsLeft(msg)-]{+ws.onopen = () =>+} {
    [-const el-]{+wsConnected+} = [-document.getElementById("ov-turnouts");-]
[-  if (!el) return;-]{+true;+}
{+    logLine("WS connected");+}
{+  };+}

  [-const online-]{+ws.onclose+} = [-!!msg?.mega2?.online;-]
[-  const t-]{+() => {+}
{+    wsConnected+} = [-msg?.mega2?.turnouts;-]{+false;+}
{+    logLine("WS disconnected");+}
{+    setTimeout(wsConnect, 1500);+}
{+  };+}

  [-if (!online || !t)-]{+ws.onmessage = (ev) => {+}
{+    let msg;+}
{+    try+} { [-el.innerHTML-]{+msg+} = [-"<em>keine Daten</em>";-]{+JSON.parse(ev.data); } catch(e) {+} return; }
    {+window.lastStateMsg = msg;+}

    [-const soll = t.sollMask ?? 0;-]
[-  const ist  = t.istMask ?? 0;-]{+if (DEBUG_WS) console.log("[WS MSG]", msg);+}

    [-const names = ["W12", "W13", "W14", "W15"];-]
[-  let html = `<div class="badge-wrap">`;-]
[-  for (let i = 0; i < 4; i++) {-]
[-    const s = bit(soll, i) ? "A" : "G";-]
[-    const r = bit(ist, i)  ? "A" : "G";-]
[-    const ok = (s-]{+if (msg.type+} === [-r);-]
[-    html += `<span class="badge ${ok ? "badge-green" : "badge-red"}">${names[i]} Soll:${s} Ist:${r}</span>`;-]{+"state") {+}
{+      renderAll(msg);+}
    }
  [-html += `</div>`;-]
[-  el.innerHTML = html;-]{+};+}
}

[-function renderBlocksLeft(msg) {-]
[-  const el = document.getElementById("ov-blocks");-]
[-  if (!el) return;-]{+/* ===================== INIT ===================== */+}

[-const online = !!msg?.mega2?.online;-]
[-  if (!online)-]{+document.addEventListener("DOMContentLoaded", () =>+} {
  [-el.innerHTML-]{+$("ui-version").textContent+} = [-"<em>keine Daten</em>";-]
[-    return;-]
[-  }-]{+UI_VERSION;+}

  [-const occMask = msg?.mega2?.blockOccupiedMask ?? 0;-]
[-  const entryNow = msg?.mega2?.entryAllowed;-]
[-  const entryPrev = msg?.mega2?.entryPreview;-]{+$("btn-power")?.addEventListener("click", sendPowerToggle);+}
{+  $("btn-m1-mode")?.addEventListener("click", sendMega1ModeToggle);+}

  [-// 1) Occupancy-]
[-  let html = `<div><b>Belegung:</b></div><div class="badge-wrap">`;-]
[-  for (let i = 0; i < 9; i++) {-]
[-    const occ = bit(occMask, i);-]
[-    html += `<span class="badge ${occ ? "badge-red" : "badge-green"}">B${i+1} ${occ ? "belegt" : "frei"}</span>`;-]
[-  }-]
[-  html += `</div>`;-]

[-  // 2) FROM→TO Signale (fixe Liste nach Topologie)-]
[-  if (Array.isArray(entryPrev) && entryPrev.length >= 9 && Array.isArray(entryNow) && entryNow.length >= 9) {-]
[-    const edges = [-]
[-      [1,2],[2,3],[3,4],[4,1],[4,5],-]
[-      [5,7],[5,8],[5,9],-]
[-      [7,6],[8,6],[9,6],-]
[-      [6,4]-]
[-    ];-]

[-    html += `<div style="margin-top:0.8rem;"><b>Signale (FROM → TO):</b></div>`;-]
[-    html += `<div class="badge-wrap">`;-]

[-    for (const [from,to] of edges) {-]
[-      const maskPrev = entryPrev[from-1] >>> 0;-]
[-      const maskNow  = entryNow[from-1] >>> 0;-]

[-      const prevOk = (maskPrev & (1 << (to-1))) !== 0;-]
[-      const nowOk  = (maskNow  & (1 << (to-1))) !== 0;-]

[-      html += `<span class="badge ${prevOk ? "badge-green" : "badge-red"}" style="line-height:1.15; padding-top:6px; padding-bottom:6px;">-]
[-        <div style="font-size:0.85em; opacity:0.85;">P: B${from}→B${to}</div>-]
[-        <div style="font-weight:700;">N: ${nowOk ? "OK" : "STOP"}</div>-]
[-      </span>`;-]
[-    }-]

[-    html += `</div>`;-]
[-  } else {-]
[-    html += `<div style="margin-top:0.8rem;"><em>Signale: keine Daten</em></div>`;-]
[-  }-]

[-  el.innerHTML = html;-]
[-}-]{+wsConnect();+}
{+});+}
